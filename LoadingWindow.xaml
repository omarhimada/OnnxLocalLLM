<Window x:Class="UI.LoadingWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Loading..." Height="255" Width="800"
        Background="Black"
        AllowsTransparency="False"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">
	<WindowChrome.WindowChrome>
		<WindowChrome CaptionHeight="30" 
		              ResizeBorderThickness="5" 
		              CornerRadius="0"
		              GlassFrameThickness="0"/>
	</WindowChrome.WindowChrome>
    <Border BorderThickness="1" BorderBrush="#333333">
        <Grid Background="Black">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Background="Black" Margin="0,0,0,0">
	            <Label Content="Loading..." HorizontalAlignment="Center" Margin="0,0,0,51" VerticalAlignment="Center" Background="Black" Foreground="White" FontFamily="Cascadia Mono ExtraLight" FontSize="16"/>
                <Label Content="Looking for local ONNX model." HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Center" Background="Black" Foreground="White" FontFamily="Cascadia Mono ExtraLight" FontSize="16"/>
                <Label Content="Attempting to load model into your machine's memory." HorizontalAlignment="Center" Margin="0,0,0,-51" VerticalAlignment="Center" Background="Black" Foreground="White" FontFamily="Cascadia Mono ExtraLight" FontSize="16"/>
	        </Grid>
        </Grid>
    </Border>
</Window>