<Window x:Class="UI.LoadingWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="Loading..."
        Background="Black"
        AllowsTransparency="False"
        WindowStyle="None"
        Width="800" Height="200"
        WindowStartupLocation="CenterScreen">
	<WindowChrome.WindowChrome>
		<WindowChrome CaptionHeight="30" 
		              ResizeBorderThickness="5" 
		              CornerRadius="0"
		              GlassFrameThickness="0"/>
	</WindowChrome.WindowChrome>
    <Border BorderThickness="1" BorderBrush="#333333" Padding="15">
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
	        <Label Content="Loading..." HorizontalAlignment="Center" Background="Black" Foreground="White" FontFamily="Cascadia Mono ExtraLight" FontSize="16"/>
	        <Label Content="Looking for local ONNX model." HorizontalAlignment="Center" Margin="0,0,0,0" Background="Black" Foreground="White" FontFamily="Cascadia Mono ExtraLight" FontSize="16"/>
	        <Label Content="Attempting to load model into your machine's memory." HorizontalAlignment="Center" Background="Black" Foreground="White" FontFamily="Cascadia Mono ExtraLight" FontSize="16"/>
        </StackPanel>
    </Border>
</Window>